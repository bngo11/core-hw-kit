libplist:
  generator: github-1
  packages:
    - libtatsu:
        github:
          user: libimobiledevice
          repo: libtatsu
          query: releases
          tarball: libtatsu-{version}.tar.bz2
